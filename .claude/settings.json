{
  "//": "Project-scoped agent hooks for Claude Code — format-on-edit and verify-at-stop, so gate refusals at commit time become rare. Merge target for the claude-hooks template.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "npx --no-install prettier --write \"$CLAUDE_FILE_PATHS\" 2>/dev/null || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm run --silent verify || echo 'verify failed — the task is not complete until it passes'"
          }
        ]
      }
    ]
  }
}
